export interface CobroResultado {
  horasCobradas: number;
  tarifaAplicada: number;
  subtotal: number;
  totalAPagar: number;
  horasAcreditadas: number;
  fecha: string;
  horaEntrada: string;
  horaSalida: string;
}
